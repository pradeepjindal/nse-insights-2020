mfiDataCountForDateSql: >
  SELECT count(*)
  FROM calc_mfi_tab t
  WHERE t.trade_date = to_date(?,'yyyy-mm-dd')

mfiDataCountForDateSql_03: >
  SELECT count(*)
  FROM calc_mfi_tab_03 t
  WHERE t.trade_date = to_date(?,'yyyy-mm-dd')

mfiDataCountForDateSql_05: >
  SELECT count(*)
  FROM calc_mfi_tab_05 t
  WHERE t.trade_date = to_date(?,'yyyy-mm-dd')

mfiDataCountForDateSql_10: >
  SELECT count(*)
  FROM calc_mfi_tab_10 t
  WHERE t.trade_date = to_date(?,'yyyy-mm-dd')

mfiDataCountForDateSql_20: >
  SELECT count(*)
  FROM calc_mfi_tab_20 t
  WHERE t.trade_date = to_date(?,'yyyy-mm-dd')


mfiDataCountForDateSqlNew: >
  SELECT count(*)
  FROM calc_mfi_tab_new t
  WHERE t.trade_date = to_date(?,'yyyy-mm-dd')


mfiSql: >
  SELECT *
  FROM calc_mfi_tab

mfiSql_03: >
  SELECT *
  FROM calc_mfi_tab_03

mfiSql_05: >
  SELECT *
  FROM calc_mfi_tab_05

mfiSql_10: >
  SELECT *
  FROM calc_mfi_tab_10

mfiSql_20: >
  SELECT *
  FROM calc_mfi_tab_20


mfiSqlNew: >
  SELECT *
  FROM calc_mfi_tab_new


